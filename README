Requirements
============

First you need to install rst2pdf

On Mac : `sudo port install py-docutils`
On ubuntu : `apt-get install python-docutils`

Compiling your resume in PDF
============================

`rst2pdf -s resume.style --font-folder=~/Library/Fonts resume.rst`
